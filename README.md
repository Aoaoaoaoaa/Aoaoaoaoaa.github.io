# RansomKains.com

Dark, aviation-forward personal portfolio for Ransom Kains.

Before publishing: replace the LinkedIn placeholder in `index.html`, add your `resume.pdf`, and replace the aviation gallery placeholders with your real photos. Keep `index.html` and `styles.css` in the same directory.
